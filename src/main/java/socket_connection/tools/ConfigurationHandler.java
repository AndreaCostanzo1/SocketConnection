package socket_connection.tools;

public class ConfigurationHandler {
    private static Configuration configuration;

    private ConfigurationHandler(){

//        configuration=new Gson().fromJson(, Configuration.class);
    }
}
